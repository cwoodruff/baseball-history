@model baseball_history_web.ViewModels.PlayerDetailViewModel

<div class="card">
    <div class="card-header card-header-mlb">
        <strong>Pitching Season by Season</strong>
    </div>
    <div class="table-responsive" style="max-height: 400px;">
        <table class="table table-sm table-baseball mb-0">
            <caption class="visually-hidden">Season-by-season pitching statistics for @Model.FullName</caption>
            <thead>
            <tr>
                <th scope="col">Year</th>
                <th scope="col">Team</th>
                <th class="text-end" scope="col">W</th>
                <th class="text-end" scope="col">L</th>
                <th class="text-end" scope="col">ERA</th>
                <th class="text-end" scope="col">G</th>
                <th class="text-end" scope="col">GS</th>
                <th class="text-end" scope="col">SV</th>
                <th class="text-end" scope="col">IP</th>
                <th class="text-end" scope="col">SO</th>
                <th class="text-end" scope="col">BB</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var season in Model.PitchingSeasons)
            {
                <tr data-team="@season.TeamId">
                    <td>@season.Year</td>
                    <td>
                        @if (!string.IsNullOrEmpty(season.TeamId) && !string.IsNullOrEmpty(season.LgId))
                        {
                            <a href="/Teams/Season/@season.TeamId/@season.LgId/@season.Year"
                               title="View the @season.Year @(season.TeamName ?? season.TeamId) season">
                                @(season.TeamName ?? season.TeamId)
                            </a>
                        }
                        else
                        {
                            @(season.TeamName ?? season.TeamId)
                        }
                    </td>
                    <td class="text-end stat-cell">@season.Wins</td>
                    <td class="text-end stat-cell">@season.Losses</td>
                    <td class="text-end stat-cell">@season.FormattedEra</td>
                    <td class="text-end stat-cell">@season.Games</td>
                    <td class="text-end stat-cell">@season.GamesStarted</td>
                    <td class="text-end stat-cell">@season.Saves</td>
                    <td class="text-end stat-cell">@season.FormattedInningsPitched</td>
                    <td class="text-end stat-cell">@season.Strikeouts</td>
                    <td class="text-end stat-cell">@season.Walks</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>

@if (Model.AdvancedPitchingSeasons.Any(s => s.Ip > 0))
{
    <div class="card mt-3">
        <div class="card-header card-header-mlb d-flex justify-content-between align-items-center">
            <strong>Advanced Pitching</strong>
            <small><a href="/Glossary" class="text-white-50">About these stats</a></small>
        </div>
        <div class="table-responsive" style="max-height: 400px;">
            <table class="table table-sm table-baseball mb-0">
                <caption class="visually-hidden">Season-by-season advanced pitching statistics for @Model.FullName</caption>
                <thead>
                <tr>
                    <th scope="col">Year</th>
                    <th scope="col">Lg</th>
                    <th class="text-end" scope="col">IP</th>
                    <th class="text-end" scope="col" title="Strikeouts per nine innings">K/9</th>
                    <th class="text-end" scope="col" title="Walks per nine innings">BB/9</th>
                    <th class="text-end" scope="col" title="Walks plus hits per inning pitched">WHIP</th>
                    <th class="text-end" scope="col" title="Season-relative qualification: one inning per team game">Qual</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var season in Model.AdvancedPitchingSeasons.Where(s => s.Ip > 0))
                {
                    <tr>
                        <td>@season.Year</td>
                        <td>@season.LgId</td>
                        <td class="text-end stat-cell">@season.FormattedIp</td>
                        <td class="text-end stat-cell">@season.FormattedK9</td>
                        <td class="text-end stat-cell">@season.FormattedBb9</td>
                        <td class="text-end stat-cell">@season.FormattedWhip</td>
                        <td class="text-end stat-cell">@(season.Qualified ? "✓" : "")</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
}
